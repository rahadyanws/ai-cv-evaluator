/**
 * @file paths.ts
 * @description Centralizes file system path constants for the application.
 */

/**
 * The directory where candidate files (CVs, Reports) are temporarily stored.
 * This path is relative to the project root.
 */
export const UPLOAD_DIR = './uploads';
